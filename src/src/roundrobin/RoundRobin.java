package roundrobin;

//import file.File;
//import pcb.*;
//import java.util.ArrayList;

public class RoundRobin {
  // public void main(String path) {
  // ArrayList<Pcb> process;
  // File f = new File();
  // Pcb pcb;
  // // process = f.loadFile(path);
  // int timeProcess = 0;
  // for (int i = 0; i < process.length(); i++) {
  // pcb = process.getLowPcb();
  // timeProcess += pcb.getEstimatedTime();
  // System.out.println("Processo do Id " + pcb.getId() + "Executado em " +
  // timeProcess);
  // process.remove(pcb);
  // }
  // System.out.println("Processos executados em " + timeProcess);
  // }
}